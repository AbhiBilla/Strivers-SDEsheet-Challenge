Node *temp = head;
    int count=0;
    while(temp){
        count++;
        temp=temp->next;
    }
    int target = count/2;
        temp=head;
        while(target--){
            temp=temp->next;
        }
    return temp
